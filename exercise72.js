/*
72. Write a JavaScript program to check if the first and last elements are equal of a given array of integers length 3.
*/

function func(array){
    return Boolean(array[0] === array[array.length - 1]);
};
